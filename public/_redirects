# Redirect HTTP to HTTPS
http://neuralflow.cloud/* https://neuralflow.cloud/:splat 301!
http://www.neuralflow.cloud/* https://neuralflow.cloud/:splat 301!

# Redirect www to non-www
https://www.neuralflow.cloud/* https://neuralflow.cloud/:splat 301!

# Legacy URL redirects (if any old URLs need to be redirected)
/home /
/index.html /
/index.php /

# API redirects for better organization
/api/v1/* /api/:splat 301

# Blog post redirects (if URL structure changed)
/blog/post/* /blog/:splat 301
/articles/* /blog/:splat 301

# Service redirects for better SEO
/service/* /services/:splat 301
/solution/* /services/:splat 301

# Contact form redirects
/contact-us /contact 301
/get-in-touch /contact 301

# About page redirects
/about-us /about 301
/company /about 301

# FAQ redirects
/help /faq 301
/support /faq 301
/questions /faq 301

# Privacy and Terms redirects
/privacy-policy /privacy 301
/terms-and-conditions /terms 301
/tos /terms 301

# Social media and external redirects
/linkedin https://linkedin.com/company/neuralflow-ai 302
/twitter https://twitter.com/neuralflow_ai 302
/github https://github.com/neuralflow-ai 302

# Download redirects (if any resources)
/download/* /resources/:splat 301

# Admin and dashboard redirects (if applicable)
/admin/* /dashboard/:splat 301
/login /dashboard/login 301

# 404 fallback - SPA routing
/* /index.html 200

# Prerender for better SEO (if using prerendering service)
/sitemap.xml /sitemap.xml 200
/robots.txt /robots.txt 200
/manifest.json /manifest.json 200

# Security redirects
/.env 404
/.git/* 404
/config/* 404
/node_modules/* 404
/.vscode/* 404
/.idea/* 404